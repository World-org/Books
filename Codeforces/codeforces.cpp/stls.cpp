#include<bits/stdc++.h>

using namespace std;

int main(){
    int t;
    cin>>t;
    while(t--){
        int n;
        vector<int> v;
        reverse(sort(v.begin(),v.end()));
        c = min(a,b);
        int a[26];
        sort(a,a+26);
        d = max(a,b);

    }
    return 0;
}
